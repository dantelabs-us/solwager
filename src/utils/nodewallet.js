// Ensure this file is used only in server-side code.
// Add a comment or refactor to avoid client-side usage.
if (typeof window === 'undefined') {
  const fs = require('fs');
  // Add your server-side code here
}